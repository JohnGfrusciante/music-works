アーティスト用チャット一覧ページ
%br
%br

%ul.artist-chatlist
  - @chat_lists.each do |chat_list|
    %li.artist-chatlist__content
      .artist-chatlist__content__title
        = link_to "案件名：#{chat_list.matter.title}", "/matters/#{chat_list.matter.id}/chats/#{chat_list.id}/reply_messages", method: :get
        - @unread_messages.find do |unread_message|
          - if unread_message.chat.id == chat_list.id
            ※未読のメッセージがあります。
        %br
        質問者：
        = chat_list.user.name
        %br
        【投稿内容】
        %br
        - @messages.each do |message|
          - if message.chat_id == chat_list.id
            - if message.user_id == current_user.id
              = message.user.musician.name
            - else
              = message.user.name
            ：
            = message.content
            = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
            %br
        %br
        %br


- if @chat_lists.blank?
  ※チャット質問は、まだ投稿されておりません。

%br
%br
= link_to "TOPページへ戻る", root_path
